---
interface DecayPage {
  url: string;
  title: string;
  peakPos: number;
  currentPos: number;
  drop: number;
  commercialValue: 'high' | 'medium' | 'low';
  refreshStatus: 'pending' | 'in_progress' | 'done' | 'none';
}

interface Props {
  counts?: { critical: number; high: number; normal: number };
  pages?: DecayPage[];
}

const { counts, pages } = Astro.props;
const c = counts || { critical: 0, high: 0, normal: 0 };
const items = pages || [];
---

<section class="panel">
  <h2>Content Decay</h2>
  <p class="subtitle">Pages losing rankings · Weekly</p>

  <div class="severity-cards">
    <div class="sev-card critical">
      <span class="sev-label">Critical</span>
      <span class="sev-value">{c.critical}</span>
    </div>
    <div class="sev-card high">
      <span class="sev-label">High</span>
      <span class="sev-value">{c.high}</span>
    </div>
    <div class="sev-card normal">
      <span class="sev-label">Normal</span>
      <span class="sev-value">{c.normal}</span>
    </div>
  </div>

  {items.length === 0 ? (
    <div class="notice"><p>No decaying content detected yet.</p></div>
  ) : (
    <div class="table-scroll">
      <table>
        <thead>
          <tr>
            <th>Title</th>
            <th>Peak</th>
            <th>Current</th>
            <th>Drop</th>
            <th>Value</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          {items.map(page => (
            <tr>
              <td class="title-cell" title={page.url}>{page.title}</td>
              <td class="mono">{page.peakPos}</td>
              <td class="mono">
                <span class={page.currentPos <= 10 ? 'pos-good' : page.currentPos <= 20 ? 'pos-ok' : 'pos-low'}>
                  {page.currentPos}
                </span>
              </td>
              <td class="mono"><span class="change-down">-{page.drop}</span></td>
              <td><span class={`badge val-${page.commercialValue}`}>{page.commercialValue}</span></td>
              <td><span class={`badge refresh-${page.refreshStatus}`}>{page.refreshStatus.replace('_', ' ')}</span></td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )}
</section>

<style>
  .panel {
    background: var(--color-deep-water);
    border: 1px solid var(--color-border);
    border-radius: 0.375rem;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
    box-shadow: 0 1px 3px var(--color-card-shadow);
  }
  h2 {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-parchment);
    margin: 0 0 0.25rem 0;
  }
  .subtitle {
    font-size: 0.8rem;
    color: var(--color-slate);
    margin-bottom: 1rem;
  }
  .notice {
    background: var(--color-frost);
    border: 1px solid var(--color-border);
    border-radius: 0.375rem;
    padding: 1rem;
    color: var(--color-slate);
    font-size: 0.85rem;
  }
  .severity-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin-bottom: 1.5rem;
  }
  .sev-card {
    border: 1px solid var(--color-border);
    border-radius: 0.375rem;
    padding: 1rem;
    text-align: center;
    background: var(--color-frost);
  }
  .sev-label {
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    display: block;
    margin-bottom: 0.25rem;
  }
  .sev-value {
    font-size: 1.5rem;
    font-weight: 700;
    display: block;
    line-height: 1.2;
  }
  .sev-card.critical .sev-label { color: var(--color-danger); }
  .sev-card.critical .sev-value { color: var(--color-danger); }
  .sev-card.critical { border-color: rgba(255, 94, 94, 0.3); }
  .sev-card.high .sev-label { color: var(--color-warning); }
  .sev-card.high .sev-value { color: var(--color-warning); }
  .sev-card.normal .sev-label { color: var(--color-slate); }
  .sev-card.normal .sev-value { color: var(--color-parchment); }
  .table-scroll { overflow-x: auto; }
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.8rem;
  }
  th {
    text-align: left;
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-slate);
    padding: 0.5rem 0.6rem;
    border-bottom: 1px solid var(--color-border);
  }
  td {
    padding: 0.45rem 0.6rem;
    color: var(--color-starlight);
    border-bottom: 1px solid var(--color-border-light);
  }
  .title-cell { max-width: 280px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .mono { font-size: 0.8rem; }
  .pos-good { color: #2a9d5c; }
  .pos-ok { color: #cc7f00; }
  .pos-low { color: var(--color-slate); }
  .change-down { color: var(--color-danger); }
  .badge {
    font-size: 0.7rem;
    font-weight: 500;
    padding: 0.15rem 0.4rem;
    border-radius: 0.25rem;
    text-transform: capitalize;
    white-space: nowrap;
  }
  .val-high { background: rgba(24, 165, 148, 0.12); color: var(--color-fleece-gold); }
  .val-medium { background: rgba(108, 117, 125, 0.12); color: var(--color-slate); }
  .val-low { background: rgba(108, 117, 125, 0.06); color: var(--color-slate); }
  .refresh-pending { background: rgba(255, 159, 0, 0.12); color: #cc7f00; }
  .refresh-in_progress { background: rgba(13, 153, 255, 0.12); color: #0d99ff; }
  .refresh-done { background: rgba(58, 201, 119, 0.12); color: #2a9d5c; }
  .refresh-none { background: rgba(108, 117, 125, 0.06); color: var(--color-slate); }
</style>
