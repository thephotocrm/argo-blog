---
interface QueryRow {
  query: string;
  clicks: number;
  impressions: number;
  ctr: number;
  position: number;
}

interface Props {
  queries?: QueryRow[];
  error?: string;
}

const { queries, error } = Astro.props;
---

<section class="panel">
  <h2>Top Queries</h2>
  <p class="subtitle">Google Search Console · Last 28 days</p>

  {error ? (
    <div class="notice">
      <p>{error}</p>
    </div>
  ) : !queries || queries.length === 0 ? (
    <div class="notice">
      <p>No GSC data available yet. Connect your Google Search Console to see query rankings.</p>
    </div>
  ) : (
    <div class="table-scroll">
      <table>
        <thead>
          <tr>
            <th>Query</th>
            <th>Clicks</th>
            <th>Impressions</th>
            <th>CTR</th>
            <th>Position</th>
          </tr>
        </thead>
        <tbody>
          {queries.map(q => (
            <tr>
              <td><code>{q.query}</code></td>
              <td class="mono">{q.clicks.toLocaleString()}</td>
              <td class="mono">{q.impressions.toLocaleString()}</td>
              <td class="mono">{q.ctr}%</td>
              <td class="mono">
                <span class={q.position <= 10 ? 'pos-good' : q.position <= 20 ? 'pos-ok' : 'pos-low'}>
                  {q.position}
                </span>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )}
</section>

<style>
  .panel {
    background: var(--color-deep-water);
    border: 1px solid var(--color-border);
    border-radius: 0.375rem;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
    box-shadow: 0 1px 3px var(--color-card-shadow);
  }
  h2 {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-parchment);
    margin: 0 0 0.25rem 0;
  }
  .subtitle {
    font-size: 0.8rem;
    color: var(--color-slate);
    margin-bottom: 1rem;
  }
  .notice {
    background: var(--color-frost);
    border: 1px solid var(--color-border);
    border-radius: 0.375rem;
    padding: 1rem;
    color: var(--color-slate);
    font-size: 0.85rem;
  }
  .table-scroll { overflow-x: auto; }
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.8rem;
  }
  th {
    text-align: left;
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-slate);
    padding: 0.5rem 0.6rem;
    border-bottom: 1px solid var(--color-border);
  }
  td {
    padding: 0.45rem 0.6rem;
    color: var(--color-starlight);
    border-bottom: 1px solid var(--color-border-light);
  }
  code { font-size: 0.8rem; color: var(--color-parchment); }
  .mono { font-size: 0.8rem; }
  .pos-good { color: #2a9d5c; }
  .pos-ok { color: #cc7f00; }
  .pos-low { color: var(--color-slate); }
</style>
